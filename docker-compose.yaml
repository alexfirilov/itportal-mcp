services:
  itportal-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    image: itportal-mcp:latest
    container_name: itportal-mcp
    restart: unless-stopped
    env_file:
      - .env
    environment:
      MCP_LISTEN_ADDR: ${MCP_LISTEN_ADDR:-:8080}
    ports:
      - "${MCP_HOST_PORT:-8080}:8080"
